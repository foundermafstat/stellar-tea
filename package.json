{
	"name": "stellar-tea",
	"type": "module",
	"version": "0.0.1",
	"packageManager": "pnpm@10.6.2",
	"private": true,
	"scripts": {
		"install:contracts": "pnpm --filter \"./packages/*\" install && pnpm --filter \"./packages/*\" run --if-present build",
		"format": "prettier . --write"
	},
	"dependencies": {},
	"devDependencies": {
		"prettier": "3.6.2"
	}
}
